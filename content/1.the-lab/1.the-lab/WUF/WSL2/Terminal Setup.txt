Terminal Setup -> https://learn.microsoft.com/en-us/windows/wsl/faq#how-can-i-transfer-my-wsl-files-from-one-machine-to-another-

to watch -> The Monkey King: Reborn
		arena wars

https://www.youtube.com/shorts/551lh10g_go -- from plan to production
https://www.youtube.com/shorts/a_yw0Z0nbng -- nver forget a comand

https://learn.microsoft.com/en-us/windows/wsl/

*** VMware doesn't support nested virtualization with AMD, meaning you cant use WSL2 on VMware. We have to configure WSL1..
check to see if your cpu supports it

Get-WmiObject -Query "select * from Win32_Processor" | Select-Object Name, VirtualizationFirmwareEnabled


## setup
- backup
	- https://chatgpt.com/share/4f9ee6ab-1811-439e-94e0-11d8171bb527
- restore
	- import tar file & remove Linux distro https://chatgpt.com/share/2d360de5-abb5-4228-9781-54bece66eddf

- install ubuntu
	- upgade to noble https://chatgpt.com/share/04ae0ec3-340c-40c0-ba7b-0a9795fb6c6b
	- terminal colors updated
	- make default
- bashrc
	- transfer bashrc files


----new way---
remove old ext4.vhdx and replace it with your backup ext4.vdhx at:

C:\Users\skkylimits\AppData\Local\Packages\CanonicalGroupLimited.Ubuntu24.04LTS_79rhkp1fndgsc\LocalState


- things to note -> see how windows credential manager should be configured in order for git to work without doing the setup again
- how does docker work then?

## install 

- apt packages
	- package installer
	- cmatrix
	- ca-certificates
	- nmap
	- ranger
	- git
	- net-tools
	- nmap
	- nc
	- sppiderfoot?
	- cargo? rust
	- cron

## configure

- install vs code
	remove shortcut shift ctrl L from radeon to let it work in vs code
Open the Keyboard Shortcut by pressing Ctrl + K then Ctrl + S and search for Collapse, you will see the Collapse Folders in Explorer key binding (It is empty by default). Press the + button to add your desired key binding.
	listed on microsft ebsite
	extensions: Remote Development
	extensions: border colors
	WSL
	inherit window size from previous closed size
		- Open Visual Studio Code.

		Access Settings:

    		On Windows/Linux: Go to File > Preferences > Settings.
    		On macOS: Go to Code > Preferences > Settings.
		- "window.newWindowDimensions": "inherit"
		- markdownlint

- setup pnpm
	- https://pnpm.io/installation - npm install -g pnpm
setup http server 
	- npm install -g http-server

- setup python manager
	- sudo apt install build-essential zlib1g-dev libncurses5-dev libgdbm-dev libnss3-dev libssl-dev libreadline-dev libffi-dev curl libbz2-dev libsqlite3-dev tk-dev liblzma-dev
	- pyenv install 3.12.3
	- curl https://pyenv.run | bash

- install whisper AI


- install nvm
	- wget -qO- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
	- export already in bash
	- nvm install --lts
	- nvm use --lts
	- nvm default v..


- configure GitHub
	- add GitHub credential manager as said in microsfot
	- create new token in guthib use that as password


- git crednential manager
	- install windows for git
	- git config --global credential.helper "/mnt/c/Program\ Files/Git/mingw64/libexec/git-core/git-credential-wincred.exe"
		- newest git still install wincred instead of git credential manager for some reason. 
	- git clone <repo>
	- git push to test if you have to enter login

	IF YOU WANT A LINUX ONLY OPPTION choose short stored cache or just plainb text. Or dive deeper into the other options

	# Installing Git Credential Manager
	download https://github.com/git-ecosystem/git-credential-manager/releases/download/v2.5.1/gcm-linux_amd64.2.5.1.deb to wsl then run
	sudo dpkg -i <path-to-package>
	sudo dpkg -i gcm-linux_amd64.2.5.1.deb
	git-credential-manager configure

	git config --global user.name "skkylimits"
	git config --global user.email "skkylimits@gmail.com
	"
	- # Configuring Git Credential Manager
	git-credential-manager configure
	
	# If it doesn't work run the following
	git config --global --get-all credential.helper
	git config --global --unset-all credential.helper
	# Setting credential helper to cache
	git config --global credential.helper cache
	# verify
	git config --global --get credential.helper

	# verify version
	git-credential-manager --version

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

visual studio code snippet: https://chatgpt.com/share/ffbfcfc2-1eab-4f97-8d03-9faa4d1267d0
keybindings.json
// Place your key bindings in this file to override the defaults
[
    {
        "key": "ctrl+shift+[",
        "command": "workbench.files.action.collapseExplorerFolders"
    },
    {
        "key": "ctrl+shift+.",
        "command": "editor.action.insertSnippet",
        "args": {
            "snippet": "console.log($1);$2"
        },
        "when": "editorTextFocus"
    }
]

terminal -colors

  {
            "background": "#202124",
            "black": "#202124",
            "blue": "#CC342B",
            "brightBlack": "#969896",
            "brightBlue": "#CC342B",
            "brightCyan": "#CC342B",
            "brightGreen": "#2AE673",
            "brightPurple": "#A36AC7",
            "brightRed": "#CC342B",
            "brightWhite": "#FFFFFF",
            "brightYellow": "#FFF508",
            "cursorColor": "#FFFFFF",
            "cyan": "#CC342B",
            "foreground": "#FFFFFF",
            "green": "#2ad46c",
            "name": "13",
            "purple": "#9764b8",
            "red": "#CC342B",
            "selectionBackground": "#FFFFFF",
            "white": "#eaeaea",
            "yellow": "#eae10c"
	"grey: "#8c8d8c"
        },


---VIRTUAL MACHINES--

TO GET WSL2 TO WORK IN HYPER-V. No solution for VMware as of yet but to switch to intel. Same goes for VirtualBox

FOLLOW THIS
https://www.luisllamas.es/en/hyper-v-nested-virtualization/

basically
Get-VM
Stop-VM -Name 'your-vm-name'
Update-VMVersion -Name 'your-vm-name'
Set-VMProcessor -VMName 'your-vm-name' -ExposeVirtualizationExtensions $True