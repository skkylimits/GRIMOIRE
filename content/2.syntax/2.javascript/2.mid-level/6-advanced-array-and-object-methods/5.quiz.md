---
title: Quiz
Description: Challenge yourself
---

## 1 Which of the following correctly describes the `Array.prototype.map()` method?

It executes a reducer function on each element of the array, resulting in a single output value.

It modifies the original array by applying a function to each element.

**It creates a new array with the results of calling a provided function on every element in the calling array.**

It returns a new array with all elements that pass the test implemented by the provided function.

## 2 Which statement about `Array.prototype.reduce()` is correct?

It always returns an array of the same length as the original.

It can only be used to sum numbers in an array.

It modifies the original array by removing elements that don't pass a test.

**It executes a reducer function on each element of the array, resulting in a single output value.**

## What does the `Object.keys()` method return?

**An array of a given object's own enumerable string-keyed property names.**

An array of a given object's own enumerable string-keyed property values.

An array of a given object's own enumerable string-keyed property key-value pairs.

A string representation of all the properties in the object.

## What is the effect of using `Object.freeze()` on an object?

It makes the object read-only, but allows new properties to be added.

It prevents new properties from being added, but allows existing properties to be modified or deleted.

**It makes the object immutable, preventing new properties from being added and existing properties from being modified or deleted.**

It makes the object immutable only at the top level, allowing nested objects to be modified.
