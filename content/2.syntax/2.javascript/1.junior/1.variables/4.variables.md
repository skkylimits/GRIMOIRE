---
title: Variables
description: Think of them as containers where you can store and label different types of information.
---

We can declare variables to store data by using the `var`, `let`, or `const` keywords.

## A real-life analogy

Variables are like containers/boxes where you can store and label different types of information. That information can be a string of characters like those that makeup your name or a number that can be used in Math equations, like your age, or a variety of other types.

::card
---
title: Variable
icon: i-ph:shipping-container-bold
to: https://javascript.info/variables
target: _blank
---
A `variable` is a “named storage” for data. We can use variables to store goodies, visitors, and other data.
::

## let

let – is a modern variable declaration, when the value is changed, the old data is removed from the box.

```js
let message = 'Hello!' // define the variable and assign the value

alert(message) // Hello!
```

## var

var – is an old-school variable declaration. Normally we don’t use it at all, but we’ll cover subtle differences from let in the chapter [The old "var"](https://javascript.info/var), just in case you need them.

```js
if (true) {
	var test = true // use "var" instead of "let"
}

alert(test) // true, the variable lives after if
```

## const

const – is like let, but the value of the variable can’t be changed.

```js
const BIRTHDAY = '18.04.1982'

const age = someCode(BIRTHDAY)
```

::note
capital-named constants are only used as aliases for “hard-coded” values. const evaluated in run-time does not need to be written in capital.
::

## Naming Things

Variables should be named in a way that allows us to easily understand what’s inside them, there are some [limitations](https://javascript.info/variables#variable-naming).

::caution
Variable naming is one of the most important and complex skills in programming. A glance at variable names can reveal which code was written by a beginner versus an experienced developer.
::

::callout
---
icon: i-heroicons-light-bulb
target: _blank
to: https://developer.mozilla.org/en-US/docs/Learn_web_development/Core/Scripting/Variables
---
[Mozilla on variables](https://developer.mozilla.org/en-US/docs/Learn_web_development/Core/Scripting/Variables)
::

## Challenge

::steps{level="4"}

#### Download the project using the button at the bottom of this page

#### Unzip the project and open it in your IDE.

#### Start up the development server and visit the page in the browser (most likely at `http://localhost:5173/`).

::note
From now on, we will exclude steps 1–3 from the challenge instructions. You can assume them for every exercise moving forward.
::

---

#### Open the file `src/main.js`

In this challenge, you’ll declare and manipulate variables using `let` and `const`.

---

#### Declare a variable called `name`

- In real life, most times your name won't change, so define it as a variable that **cannot** be changed.

::note
🤔 **THINK:** Should you use `let` or `const`?
::

- Set its value to your name.
- Log the value of `name` to the console.
- Try to update the value of `name` (maybe to a nickname you're fond of?).
- Observe what happens when you run the code.

	::foldable
	#title
	Answer

	#content
	![update-const](/content/2.syntax/2.javascript/1.variables/4.variables/update-const.png)
	::

- 💡 *(You can remove this update after answering this question.)*

---

#### Declare a variable called `age`

- Your age changes every year, so define it as a variable that **can** change.

::note
🤔 **THINK:** Should you use `let` or `const`?
::

- Set its value to your current age.
- Log the value of `age` to the console.
- Update the value of `age` to the age you'll be on your next birthday.
- Log the new value of `age`.

::

> ⚠️ Reassigning name should throw a TypeError in the console; updating age should work without errors.

<br />

::card-group
   <UButton icon="i-lucide-download" size="xl" color="primary" variant="solid">Download Challenge Starting Point</UButton>
   <UButton icon="i-lucide-download" size="xl" color="primary" variant="outline">Download Challenge Solution</UButton>
::
