---
title: Code Strucutre
description: Something we all need in our life.
---

## Building Blocks Of Code

::note
In many spoken languages, there are rules for when to add periods, commas, capital letters and so on. JavaScript also has standard code structure rules you should follow to write functional and easy to read JavaScript programs.
::

::callout
---
icon: i-solar:structure-bold
target: _blank
to: https://javascript.info/structure
---
[Read](https://javascript.info/structure)
::

## Statements

These are 2 statements.

```js
alert('Hello'); alert('Universe');
```

Usually, statements are written on separate lines to make the code more readable:

```js
alert('Hello');
alert('Universe');
```

## Semicolon

A semicolon may be omitted in most cases when a line break exists.

```
alert('Hello')
alert('Universe')
```

Here, JavaScript interprets the line break as an “implicit” semicolon. This is called an [automatic semicolon insertion](https://tc39.github.io/ecma262/#sec-automatic-semicolon-insertion).

::caution
In most cases, a newline implies a semicolon. But “in most cases” does not mean “always”!
::

::callout
---
icon: i-heroicons-light-bulb
target: _blank
to: https://javascript.info/structure#semicolon
---
[The art of semicolon](https://javascript.info/structure#semicolon)
::

## Comments

As time goes on, programs become more and more complex. It becomes necessary to add comments which describe what the code does and why.

Comments can be put into any place of a script. They don’t affect its execution because the engine simply ignores them.

### One-line

`One-line comments start with two forward slash characters //.`

The rest of the line is a comment. It may occupy a full line of its own or follow a statement.

Like here:

```js
// This comment occupies a line of its own
alert('Hello');

alert('Universe'); // This comment follows the statement
```

### Multiline

`Multiline comments start with a forward slash and an asterisk /* and end with an asterisk and a forward slash */.`

Like this:

```js
/* An example with two messages.
This is a multiline comment.
*/
alert('Hello');
alert('World');
```

### No Nesting

::caution
There may not be `/*...*/` inside another `/*...*/`.
::

Such code will die with an error:

```
/*
  /* nested comment ?!? */
*/
alert( 'Universe' );
```

## Hotkeys

In VScode we simply have a hotkey assigned to :kbd{value="ctrl"} :kbd{value="/"}

## Code Quality

Later there will be a chapter called [Code quality](https://javascript.info/code-quality) which will improve on what we know.


::callout
---
icon: i-heroicons-light-bulb
target: _blank
to: https://javascript.info/comments
---
[Touch comments in more depth](https://javascript.info/comments)
::
