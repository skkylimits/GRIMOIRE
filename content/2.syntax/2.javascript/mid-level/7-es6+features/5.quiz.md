---
title: Quiz
Description: Challenge yourself
---

## 1  will be the value of `b` after executing the following code?

const [a, b, ...rest] = [10, 20, 30, 40, 50];

[10, 20]

[20, 30, 40, 50]

**20**

undefined

## 2 Given the following code, what will be logged to the console?

```
const { name: fullName = "Anonymous", age } = { age: 30 };
console.log(fullName);
```

{ name: 'Anonymous' }

**"Anonymous"**

undefined

null

## 3 What is the output of the following code?

```
const parts = ["shoulders", "knees"];
const lyrics = ["head", ...parts, "and", "toes"];
console.log(lyrics.length);
```

4

**5**

3

## 4 What will be logged to the console when the following function is called with `sum(1, 2, 3, 4, 5)`?

```
function sum(...numbers) {
  return numbers.reduce((acc, num) => acc + num, 0);
}
```

10

**15**

Error: numbers is not iterable

[1, 2, 3, 4, 5]

## 5 Which of the following correctly uses a template literal to create a string that includes the value of the variable `name`?

`Hello, {name}!`

Hello, ${name}!

**`Hello, ${name}!`**

'Hello, ' + name + '!'

## 6 Which of the following correctly exports multiple functions from a module?

None of these

function foo() {}
function bar() {}
module.exports = { foo, bar };

export default {
  foo: function () {},
  bar: function () {},
};

**function foo() {}**
**function bar() {}**
**export { foo, bar };**

## 7 Given a module `math.js` with the following content:

```
export const PI = 3.14159;
export function square(x) {
  return x * x;
}
export default function sum(a, b) {
  return a + b;
}
```

### Which of the following correctly imports the square function and the sum function?

None of these

const { sum, square } = require("./math.js");

**import sum, { square } from "./math.js";**

import { square, sum } from "./math.js";
