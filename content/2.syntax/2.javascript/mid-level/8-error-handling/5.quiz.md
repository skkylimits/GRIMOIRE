---
title: Quiz
Description: Challenge yourself
---

## 1 Which of the following correctly defines a custom error type in JavaScript?

const CustomError = {
  name: "CustomError",
  message: "This is a custom error",
};

function CustomError(message) {
  this.message = message;
}

const CustomError = new Error("This is a custom error");

```js[correct.js]
class CustomError extends Error {
  constructor(message) {
    super(message);
    this.name = "CustomError";
  }
}
```

## 2 Given the following code, what will be the order of the console.log outputs?

```js[correct.js]
try {
  throw new Error("An error occurred");
} catch (error) {
  console.log("Caught an error");
} finally {
  console.log("Finally block executed");
}
```

console.log("After try-catch-finally");

"Finally block executed", "After try-catch-finally"

**"Caught an error", "Finally block executed", "After try-catch-finally"**

"An error occurred", "Caught an error", "Finally block executed", "After try-catch-finally"

"Caught an error", "Finally block executed"
