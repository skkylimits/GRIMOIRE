---
title: Quiz
Description: Challenge yourself
---

## 1 What is the primary purpose of using Promises in JavaScript?

**To handle asynchronous operations and avoid callback hell**

To replace functions entirely

To create synchronous code blocks

To make the code run faster

## 2 What does this function do?

```
async function fetchData() {
  const response = await fetch("https://api.example.com/data");
  const data = await response.json();
  return data;
}
```

**It asynchronously fetches data from an API and parses the JSON response**

It synchronously fetches data from an API

It sends data to an API

It creates a new API endpoint

## 3 What is the output of the following code?

```
const promise = new Promise((resolve, reject) => {
  setTimeout(() => resolve("Success!"), 1000);
});

promise.then((result) => console.log(result));
console.log("End");
```

**End**
**Success!**

End

Success!
End

Success

## 4 What does JSON stand for and what is its main purpose in web development?

Java Standard Operation Nodes: To standardize JavaScript operations

Java Script Object Notation: To create Java objects in JavaScript

JavaScript Output Network: To manage network connections

**JavaScript Object Notation: To transmit data between a server and web application**

## 5 What is the primary use of the `fetch()` function in JavaScript?

To search for specific elements in an array

**To make HTTP requests to servers and retrieve resources**

To find and replace text in a string

To retrieve local files from the user's computer
