---
title: Quiz
Description: Challenge yourself
---

## 1 Which method is used to attach an event handler to an element?

element.addEvent('click', handleClick);

**element.addEventListener('click', handleClick);**

element.attachEvent('click', handleClick);

element.onEvent('click', handleClick);

## 2 What is the correct way to remove an event listener from an element?

element.removeEvent('click', handleClick);

element.detachEvent('click', handleClick);

element.deleteEventListener('click', handleClick);

**element.removeEventListener('click', handleClick);**

## 3 What does event bubbling refer to in the context of DOM events?

Events are only handled by the element they are directly triggered on

**Events are first captured and handled by the innermost element, then propagated to outer elements**

None of these

Events are simultaneously triggered on all nested elements

## 4 Which property of the event object gives you the element that triggered the event?

event.currentTarget

event.source

**event.target**

event.element

## 5 Which method is used to prevent the default behavior of an event?

**event.preventDefault();**

event.stopPropagation();

event.stop();

event.cancelDefault();
