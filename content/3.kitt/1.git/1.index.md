---
title: Git Started
description: Further expand and solidify your git knowledge 📚
---

## .git directory

In vscode settings. 

Press :shortcut{value="SHIFT"} :shortcut{value="meta"} :shortcut{value="P"} and type: Preference Open: Settings (UI)

Search for `Files: Exclude` and remove `**/.git`


## U & A

U = Untracked

A = Tracked

## Add Files

Add an entire working directory to the staging area:

```[Terminal]
git add .
```

## Remove Files

Remove a file from the staging area:

```[Terminal]
git reset .
```

## Commit Files

Commit staged files to a repository

```[Terminal]
git commit -m "chore: init"
```

Tip: Add files and commit in a single command

```[Terminal]
git commit -am "additional commit"
```

Bonus: Make an alias

```[Terminal]
git config --global alias.ac "commit -am"
```

Now run:

```[Terminal]
git ac "noice!"
```

## Git Logs

```[Terminal]
git log
```

For a more readable version:

```[Terminal]
git log --graph --oneline --decorate
```

## Git Bisect

Performs a binary search on yourr repo

```[Terminal]
git bisect start
```

Use on every good commit:

```[Terminal]
git bisect good
```

Continue this until you've found the bad apple then:

```[Terminal]
git bisect bad
```

## VSCODE TIPS

Don't forget the source control tab in vscode.

It's a hidden gem. The burger menu gives you an overview of all the possible git commands and an easy to use UI to see the diff in your files.

## Gitlens

Useful when working with bigger teams
