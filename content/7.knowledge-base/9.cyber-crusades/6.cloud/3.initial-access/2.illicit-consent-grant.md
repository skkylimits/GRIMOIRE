---
tile: Illicit Consent Grant
description: Illicit consent grant attacks exploit user-granted permissions. Entra ID allows user consent for low-impact apps, but high-impact apps need admin consent.
---

![minecraft](/content/7.knowledge-base/8.cyber-crusades/6.cloud/3.initial-access/2.illicit-consent-grant-attack/minecraft.png)

::callout
---
icon: i-heroicons-light-bulb
target: _blank
to: https://graphpermissions.merill.net/permission/
---
[See a nice and structured overview of graph permissions](https://graphpermissions.merill.net/permission/)
::

## Permissions Requested
Regardless of the platform, applications must request user permissions. For example, when signing in, an app may ask:
*"Would you like to allow this application to read your profile?"*

> You've likely seen this when logging into the lab portal with your Google account—it asked for consent to access your profile. Or any other similar platform.

![permissions-requested](/content/7.knowledge-base/8.cyber-crusades/6.cloud/3.initial-access/2.illicit-consent-grant-attack/permissions-requested.png)

::warning
As a proof of concept, when you granted access, in theory, the app could read all your emails, access your files, or even delete them. That’s the power of consent.
::

Of course, we wouldn’t actually do that! But this highlights why **consent is critical in OAuth-based applications**, and Microsoft has recognized this.

::callout
---
icon: i-heroicons-light-bulb
target: _blank
to: https://learn.microsoft.com/en-us/entra/identity/enterprise-apps/configure-user-consent?pivots=portal
---
[Learn how to properly configure user consent](https://learn.microsoft.com/en-us/entra/identity/enterprise-apps/configure-user-consent?pivots=portal)
::

## **Types of Permissions**

Microsoft realised that  that can be abused to the max. That's why if an application requests high-privileged permissions, a normal Entra ID user can only grant consent for low-impact permissions.

> There are 3 types of permissions. Low, medium and high. Microsoft designed it this way to limit potential risks.

### 1. **Low Impact Permissions (User-Granted)**
   - `Regular users can only grant consent to low-impact permissions.`
   - Only the permissions that don't need admin consent can be classified as
low impact
   - Example: [Basic sign-in](https://learn.microsoft.com/en-us/entra/identity/enterprise-apps/configure-permission-classifications?pivots=portal) (allows an app to read your email from the authentication token).
   - Another example: **User.ReadBasic.All**—lets an app read basic details (name, email, open extensions and profile photo) of all users in the tenant! It's a very interesting low impact permission.

::important
- I'm simplifying this, but the way it works for the lab portal or most basic sign-ins is like this:

    - When you consent to the lab portal (enterprisesecurity.io) for basic sign-in, the application gets the JWT (token) that you submit when you consent. It then reads the email from the token because, in basic sign-in, it has permission to access your email. The app checks the email against the database to see if the user has registered for the course.

That's the basic idea behind most sign-ins.
::

### 2. **High/Medium Impact Permissions (Admin-Granted)**
   - `Permissions that affect multiple users or resources require admin consent.`
   - **Global Administrator, Application Administrator, Cloud App Administrator,** or **"custom roles with permission-granting privileges"** can approve these.
   - Example: Allowing an app to read **all OneDrive or SharePoint files** requires admin consent.

### **3. Application Vs. Delegated Permissions**

When configuring API permissions for an application, there are two primary types:

- **Delegated Permissions:**
  - `Runs **on behalf of a signed-in user**.`
  - Requires user consent (or admin consent for high-impact permissions).
  - Limited by the signed-in user's permissions. (User's scope)

- **Application Permissions:**
  - `Always requires **admin consent**.`
  - Used when an application runs as a daemon or background service.
  - Does not require a user to be signed in.

![app-privileges-illustration](/content/7.knowledge-base/8.cyber-crusades/6.cloud/3.initial-access/2.illicit-consent-grant-attack/app-privileges-illustration.png)

::callout
---
icon: i-heroicons-light-bulb
target: _blank
to: https://learn.microsoft.com/en-us/graph/permissions-reference
---
[Microsoft Graph permissions reference](https://learn.microsoft.com/en-us/graph/permissions-reference)
::

## **Default Consent Settings**

- **Default Setting** → **Allow user consent for all apps** (Not recommended but enabled by default).
- **Recommended Setting** → **Allow user consent only for verified publishers** (Reduces risk but increases IT workload).

To check or modify [these](https://entra.microsoft.com/#view/Microsoft_AAD_IAM/ConsentPoliciesMenuBlade/~/UserSettings) policies settings:
1. Go to **Enterprise Applications** in Entra ID.
2. Navigate to **Security** → **Consent & Permissions**.

Many organizations **stick with the default setting**, knowingly or unknowingly, due to convenience.

::warning
The "Allow user consent for all apps" setting is **not recommended** but is enabled by default, and it is **abusable**.

If an organization allows user consent for all apps, employees can grant apps access to basic information in their profiles (e.g., display name, email). If an organization doesn't restrict this This could potentially lead to unauthorized access if not properly controlled.
::

## The Attack

![the-attack](/content/7.knowledge-base/8.cyber-crusades/6.cloud/3.initial-access/2.illicit-consent-grant-attack/the-attack.webp)

### **App Registration**

1. **Create the App Registration**
   - Name the app registration.
   - Register a new application in **Azure AD** as **multi-tenant**.
   - Specify the redirect URI (e.g., Web URL).

2. **Create a Secret**
   - Generate a client secret to authenticate the app.

3. **Assign API Permissions**
   - Choose between **Delegated** and **Application** permissions.
   - Level 1:
     - Delegated: User.ReadBasic.All
     - Application: Requires **admin consent** before use.
	- Level 2:

| Permission                      | Description                                                | Admin Consent Required |
|---------------------------------|------------------------------------------------------------|------------------------|
| Files.ReadWrite.All             | Have full access to all files user can access              | No                     |
| Mail.Read                       | Read user mail                                             | No                     |
| MailboxSettings.ReadWrite       | Read and write user mailbox settings                       | No                     |
| Notes.Read.All                  | Read all OneNote notebooks that user can access            | No              	    |
| User.Read                       | Sign in and read user profile                              | No                     |
| User.ReadBasic                  | Read all users' basic profiles                             | No                     |

4. **Configure Token Settings**
   - In the **Authentication** option of the **studentx** app, check **"Access tokens (used for implicit flows)"**.
   - Click on **Save**.

   *(Note: If you want to use Refresh tokens, no additional configuration is required.)*

::warning
- Logs will clearly show **"Consent to Application"** activity. making it difficult to stay undetected.
- Targeting multi-tenant environments increases detection risk.
- More success may come from internal attacks within a compromised tenant, as logs may be ignored.
::

### 365 stealer

#### Installation

[365-stealer](https://github.com/skkylimits/365-Stealer) This tool, written in Python3, automates illicit consent grant attacks. When the victim grants consent, it captures their Refresh Token, which can be used to request multiple tokens for accessing data like emails, notes, and files from OneDrive.

We wll be using 365-stealer.py for demo purposes. But there's tons more out there. A honorary mention would be [PwnAuth](https://github.com/mandiant/PwnAuth) by mandiant.

::card-group
  ::card
  ---
  title: 365-stealer.py
  icon: i-game-icons:prisoner
  to: /knowledge-base/cyber-crusades/cloud/initial-access/365-stealer
  target: _blank
  ---
  Click here for installion instructions.
  ::
  ::card
  ---
  title: PwnAuth
  icon: i-devicon:oauth
  to: https://github.com/mandiant/PwnAuth
  target: _blank
  ---
  Click here for installation instruction
  ::
::

#### Phishing

> Obtain the users data from `User.ReadBasic.All` and create access and refresh tokens.

This is the link that would be generated.

```
https://login.microsoftonline.com/common/oauth2/authorize?
response_type=code&
client_id=[client-id]&
scope=https%3A%2F%2Fgraph.microsoft.com%2F.default+openid+offline_access+&
redirect_uri=https%3A%2F%2Flocalhost%2Flogin%2Fauthorized&
response_mode=query
```

| **Parameter**                 | **Description**                                                                                       |
|-------------------------------|-------------------------------------------------------------------------------------------------------|
| `response_type=code`           | Must be **code** for authentication flow. (Can be used to request access & refresh tokens)            |
| `client_id=[client_id]`        | The Application (Client) ID of the registered app.                                                   |
| `scope=https://graph.microsoft.com/.default openid offline_access` | The requested Microsoft Graph permissions and additional scopes like openid and offline_access.       |
| `redirect_uri=https://localhost/login/authorized` | The URI where the authorization response will be sent.                                              |
| `response_mode=query`          | Specifies that the authorization code will be returned as a query parameter.                          |

::warning
You could potentially expose your attackers network if you didn't mask you redirecr URI properly. They can see where you're phishing server would be. Optimally you'd run the 365-stealer in azure.
::

> Because this is a phishing attack. Even if there would be a second factor of authentication. You'd still get an access token!

::caution
Scary and lethal attack, but not a very silent one. You'd see a log illustrated below.
::

![consented](/content/7.knowledge-base/8.cyber-crusades/6.cloud/3.initial-access/2.illicit-consent-grant/consented.png)

### Spearphishing

> Since we've successfully gained access through a regular user account, we now leverage [**OSINT**](/knowledge-base/osint) to gather more insights into the organization and identify additional attack vectors. In this case, we discover on LinkedIn that an application administrator named **Mark** could be a valuable target.

With this new information, it's crucial to review and adjust permissions for the next phase of the attack. We either update the existing app registration or create a new one with the following permissions:

- `Mail.Read`
- `MailboxSettings.ReadWrite`
- `Notes.Read.All`
- `Files.ReadWrite.All`
- `User.Read`
- `User.ReadBasic.All`

::warning
Everytime we adjust the permission scope, we have the get the users consent. Meaning we have to resend the phishing link.
::

Finally, remember to **terminate the 365.stealer.py session** and rerun it to ensure the updated permissions take effect.

::important
It doesn't matter whom we target, it's about the permissions that that target has. It could be a GA for that matter. If you only have low level permissions. You have to deal with that first. What we asked for, what the user consented to.
::

::tip
Can we go directly for a high priveleged user? Yes we can, but it's not the recommended approach. Always go for the low priv users first and map out the organization.
::

When decoding the obtained token. See if there's CAE going on by checkeing the json object:

```json
{
	"xms_cc": [
		"CP1"
	]
}
```

### Payload

> Macro infested wordfile. We're going to super simplify this, because this is not the focus of the class.

::warning
Don't use this in a real engagement. It's going to be really noisy, use a state-zero or an implant instead.
::

Prerequisites:

- Machine with office licensing

### Reverse shell

::warning
It's really noisy. Don't use that in real assesment. GO for a state zero or an implant.
::
### Persistence

## **Defenses**

::caution
- The most visible part of such an attack is the **redirect URI**.
- If an attacker gains user consent to a malicious app, they can access sensitive data.
::

- **Prevent Users from Consenting to Unverified Applications**
  - Restrict users from approving apps from **unverified publishers**.
  - This is the most effective and **low-cost** defense.

- **Enable Detection & Alerts**
  - Create an **alert for `User.ReadBasic.All`** permission grants.
  - Monitor admin consent activity, as **admin-granted permissions pose a higher risk**.

- **Advanced Security Measures**
  - Organizations with **large security budgets** can implement automatic remediation.
  - Regularly audit **OAuth consent logs** to detect unusual app approvals.
