---
title: Stealing Tokens
description: Steal like your life depends on it.
---

> You won't always be able to get tokens manually unless you have an interactive session or can use a tool that mimics the authentication process, that's why you need to steal them.

::warning
Stay away from [`user identities`](/knowledge-base/cyber-crusades/cloud/genesis/entra-id#what-are-user-identities). If there is one OPSEC trick that you would like to take away from this class, absolutely nothing else. Let's say nothing makes sense to you.
But you still want to take something away from this class
stay away from user identities. They are very closely observed. Chances of detection are high.
::

::caution
You can use refresh token to get new access tokens. **Refresh tokens** allow you to obtain new access tokens once they expire. However, accessing user identities or extracting tokens from user machines is not recommended due to high detection risks.
::

If you really really are set on stealling them from use identities even without this warning. Here are some ways.

## **Azure CLI**

> **Older Versions (Before 2.30.0 - January 2022)**

- Older versions of Azure CLI **stored access tokens in clear text** inside `accessTokens.json`.
- **📁 File Location:**

  You can dynamically retrieve the path of `accessTokens.json` in PowerShell using:
  ```
  C:\Users\[username]\.Azure\accessTokens.json
  ```

  ```powershell
  Join-Path $env:USERPROFILE ".Azure\accessTokens.json"
  ```

::tip
If you have **read access** to this file, you can extract tokens and reuse them.
::

- The same directory contains `azureProfile.json`, which holds **subscription details**.

- **Modifying Tokens:**
  - Attackers can replace or inject tokens into `accessTokens.json` to gain access via Azure CLI or Az PowerShell.
- **Clearing Stored Tokens:**
  - Run `az logout` to remove stored access tokens.

  ```powershell
  az logout
  ```

## **Azure PowerShell**

> **Older Versions of Az PowerShell**

- Stored access tokens in **clear text** inside `TokenCache.dat`.
- **📁 File Location:**
  ```
  C:\Users\[username]\.Azure\TokenCache.dat
  ```
  ```powershell
  Join-Path $env:USERPROFILE ".Azure\TokenCache.dat"
  ```
- If authentication used a **service principal**, `AzureRmContext.json` stored **ServicePrincipalSecrets in clear text**.

  ```
  %USERPROFILE%\AppData\Local\AzureRmContext #Windows
  ```
  ```
  ~/.azure/AzureRmContext #macOS and Linux
  ```

- **Saved Sessions:**
  - Users can persist authentication using `Save-AzContext`, are saved locally in a hidden folder in the user's home directory `AzureRmContext.json`.
  - Running `Disconnect-AzAccount` clears saved tokens.
  ```powershell
  Disconnect-AzAccount
  ```

## **Memory Dumps**

**Powershell**

PowerShell is a powerful scripting environment that can store credentials and tokens in its memory. By dumping the process memory, you might be able to recover sensitive data such as access tokens or passwords.

**Graph API Tokens**

Office applications like Outlook, Word, Excel can store tokens used for accessing services such as Office 365 or Graph API. These tokens are often stored in the memory of running Office apps and can be exposed by a memory dump.

### **Extraction Methods**

#### **Using Task Manager**

**Extracting Memory Dump:**

- **Step 1:** Press **Ctrl + Shift + Esc** to open **Task Manager**.

- **Step 2:** Go to the **Processes** tab and locate the process you wish to dump (e.g., PowerShell, Outlook).

- **Step 3:** Right-click on the target process, then select **Create Dump File**.

- **Step 4:** Task Manager will generate the dump file, and you'll receive a notification showing the file's location (typically in your **Temp** folder).

  You can find it under:
  ```
  C:\Users\<your_user>\AppData\Local\Temp\<dump_file_name>.dmp
  ```

#### **Using procdump.exe**

**Extracting Memory Dump:**

- **Step 1:** Download and install the Sysinternals Suite from Microsoft, which includes **procdump.exe**. You can get it from the [official Sysinternals site](https://docs.microsoft.com/en-us/sysinternals/downloads/procdump).

- **Step 2:** Open a command prompt with **elevated privileges** (Run as Administrator).

- **Step 3:** Navigate to the folder where `procdump.exe` is located or add the path to the System Environment Variables for easier access.

- **Step 4:** Run the following command to capture a dump from a target process:

  ```bash
  procdump -ma <process_id> <output_file.dmp>
  ```

  - Replace `<process_id>` with the PID (Process ID) of the process you want to dump (e.g., PowerShell, Outlook, etc.).
  - Replace `<output_file.dmp>` with the desired name for the dump file (e.g., `powershell_memory.dmp`).

  To find the process ID (PID) of a running process:
  - Open **Task Manager** and go to the **Details** tab. Locate your target process (e.g., PowerShell) and note the **PID** column.
  - or use `tasklist /v`

### **Analyzing Dumps**

After obtaining the memory dump from the `procdump` command, you can analyze it for readable strings, authentication tokens, or other sensitive information. Below are various methods and tools to help you analyze the dump effectively.

#### **1. Using Strings**

You can start by extracting readable strings from the dump file to identify patterns that may indicate access tokens, credentials, or other sensitive data.

**Steps to use the `strings` command**:

- **Step 1:** Open a **Command Prompt** (CMD) or **PowerShell** window.

- **Step 2:** Use the `strings` command to extract readable strings from the dump file:

  ```bash
  strings <dump_file_path>
  ```

  Replace `<dump_file_path>` with the full path to the memory dump you created (e.g., `C:\Users\<your_user>\Documents\outlook_crash.dmp`).

- **Step 3:** Review the output, which will list readable strings from the dump file. Look for any patterns related to access tokens, authentication data, or credentials.

#### **2. Using a Hex Editor (VSCode)**

You can use **Visual Studio Code** as a hex editor to manually search through the dump file.

**Steps to use VSCode as a Hex Editor**:

- **Step 1:** Open **Visual Studio Code**.

- **Step 2:** Install the **Hex Editor** extension from the Visual Studio Marketplace.

  - Go to Extensions (the square icon on the sidebar), search for **"Hex Editor"**, and install it.

- **Step 3:** Once installed, open your memory dump (`outlook_crash.dmp`) in VSCode.

- **Step 4:** You can now scroll through the hex view of the dump. Use the search functionality (Ctrl + F) to find keywords like `token`, `auth`, `access`, or other relevant strings that could point to sensitive information.

#### **3. Using AzureTokenExtractor for Human-Readable Output**

For a more structured analysis, especially when dealing with Azure-related tokens, you can use the **AzureTokenExtractor** tool to extract and decode Azure context data from the memory dump.

**Steps to use AzureTokenExtractor**:

- **Step 1:** Download the **AzureTokenExtractor** tool from GitHub:

  [AzureTokenExtractor on GitHub](https://github.com/LaresLLC/AzureTokenExtractor)

- **Step 2:** Once you have the tool, run the following command in the terminal to extract Azure tokens and context from the dump file:

  ```bash
  python azure-token-extractor.py -d <dump_file_path> -o <output_file.json>
  ```

  Replace:
  - `<dump_file_path>` with the path to the memory dump you created (e.g., `C:\Users\<your_user>\Documents\outlook_crash.dmp`).
  - `<output_file.json>` with the desired output file (e.g., `ExtAzureContext.json`).

- **Step 3:** The tool will process the dump and output the decoded data into a human-readable format (JSON file). You can open the JSON file to review any extracted tokens, such as authentication tokens, API keys, or Azure-related data.

#### **4. Using Forensic Analysis Tools (Volatility)**

If you require a more advanced and forensic approach to analyzing the memory dump, you can use tools like **Volatility**.

**Steps to use Volatility**:

- **Step 1:** Download and install **Volatility** from [the official website](https://www.volatilityfoundation.org/).

- **Step 2:** Open a **Command Prompt** or **PowerShell** window with **administrator privileges**.

- **Step 3:** Use Volatility to analyze the dump file with the appropriate plugin. For example, use the `pslist` plugin to list running processes or `tokens` to extract potential authentication tokens.

  Example command:

  ```bash
  volatility -f <dump_file_path> --profile=<profile_name> pslist
  ```

  - Replace `<dump_file_path>` with the path to the memory dump file.
  - Replace `<profile_name>` with the correct memory profile (e.g., `Win10x64_18362` for Windows 10 x64 version 18362).

- **Step 4:** Review the output to identify any relevant tokens, API keys, or other sensitive information present in the memory dump.

::callout
---
icon: i-heroicons-light-bulb
target: _blank
to: https://www.lares.com/blog/hunting-azure-admins-for-vertical-escalation-part-2/
---
[Read more on how to extract tokens](https://www.lares.com/blog/hunting-azure-admins-for-vertical-escalation-part-2/)
::
## Console History

Running `Get-PSReadLineOption` will display various settings, including **HistorySavePath**, which reveals the exact location of the history file.

```powershell
Get-PSReadLineOption
```

📂 **Default Location:**
```plaintext
C:\Users\%username%\AppData\Roaming\Microsoft\Windows\PowerShell\PSReadLine\ConsoleHost_history.txt
```

- Modern Windows OS **stores a persistent history** of executed PowerShell commands.
- This history is managed by the **PSReadLine module** and is saved to a file on disk.
- To locate and read the history file in PowerShell, use:
  ```powershell
  Get-Content (Get-PSReadLineOption).HistorySavePath
  ```

::caution
The **history file is stored in clear text**, meaning it can be accessed by an administrator or any user with sufficient privileges.

This file is **reboot persistent**, meaning it retains command history even after system restarts.

It may contain **sensitive credentials** if commands involving secrets, passwords, or tokens were executed.
::

- **Disable PowerShell history logging**:
  ```powershell
  Set-PSReadlineOption -HistorySaveStyle SaveNothing
  ```

## Phishing

The most effective method for stealing tokens is through phishing. You can either manually craft phishing links or use one of the following tools for streamlined attacks.

::card-group
  ::card
  ---
  title: 365-stealer.py
  icon: i-game-icons:prisoner
  to: /knowledge-base/cyber-crusades/cloud/initial-access/365-stealer
  target: _blank
  ---
  Click here for installion instructions.
  ::
  ::card
  ---
  title: PwnAuth
  icon: i-devicon:oauth
  to: https://github.com/mandiant/PwnAuth
  target: _blank
  ---
  Click here for installation instruction
  ::
  ::card
  ---
  title: EvilGinx2.0
  icon: i-ic:baseline-phishing
  to: /knowledge-base/cyber-crusades/cloud/initial-access/evilginx2.0
  target: _blank
  ---
  Click here for installion instructions.
  ::
  ::card
  ---
  title: Modlishka
  icon: i-game-icons:evil-tower
  to: https://github.com/drk1wi/Modlishka
  target: _blank
  ---
  Click here for installation instruction
  ::
::

## Browser

Can I use a token in the browser?

You cannot use a token in the browser to access the Azure portal. However, you can do it the other way around — you could potentially steal a token from the browser(`url` or `cookies`) and use it elsewhere.

## **Workload Identities**

Workload identities are special accounts used by apps or services to access resources, kind of like how a robot needs a key to open doors without needing a person to do it.

::caution
Workload identities are particularly vulnerable because it's challenging to implement MFA on service accounts for enforcement, and even in those cases, workload identities and service applications are often exempted from MFA requirements.
::

::important
Starting Tuesday, 15th, MFA enforcement will apply, but workload identities like service principals and applications  even in this case, will still be exempted . The recommendation is to implement a conditional access policy for these identities.
::

In CA there is no option to add workload identies only groups or users/devices. When you are feeling it, and want to secure your workload identity. You'd be surprised there's no option to secure that initially:

![no-workload-identities](/content/6.knowledge-base/8.cyber-crusades/6.cloud/3.initial-access/6.stealing-tokens/no-workload-identities.png)

This is how Microsoft makes profit. By forcing you to buy an addon. Called [Workload ID Premium plan](https://learn.microsoft.com/en-gb/entra/workload-id/workload-identities-faqs?WT.mc_id=Portal-Microsoft_Azure_Support#what-are-workload-id-premium-features-and-which-are-free)

> - **Microsoft has limited protection unless premium licenses are used.**

- **Premium Workload Identity License:** Required for every app/service principal.
  - Costs around **$15 per month per identity.**

![workload-identities-premium](/content/6.knowledge-base/8.cyber-crusades/6.cloud/3.initial-access/6.stealing-tokens/workload-identities-premium.png)

::foldable
#title
Why Avoid User Identities?

#content
- **User identities are closely monitored** and have a higher chance of detection.
- **Access tokens expire faster** than those issued to workload identities.
- **Instead, focus on**:
  - **Managed identities** (used by applications, VMs, etc.).
  - **Service principals** (used for automation and app authentication).
::

## **Defenses**
- **Update Azure CLI & Az PowerShell** to latest versions.
- **Use `az logout` and `Disconnect-AzAccount` to clear stored tokens.**
- **Disable PowerShell history logging**:
  ```powershell
  Set-PSReadlineOption -HistorySaveStyle SaveNothing
  ```
- **Monitor for unauthorized API requests and unusual token usage.**
- **Enforce conditional access policies on workload identities (requires Premium license).**

## **Final Thought**
- **Stealing access tokens from Azure CLI and PowerShell is possible in older versions.**
- **User identities are risky** due to short token lifetimes and high detection chances.
- **Workload identities (service principals, managed identities) are a better target.**
- **PowerShell history (PSReadLine) can reveal sensitive credentials.**
- **Memory dumps of running processes (Office 365, PowerShell) can contain access tokens.**
- **Tokens can be used to access ARM and Microsoft Graph APIs.**
