---
title: Overview
description: Glossary of DNS terms.
---

> This will save you from goin six months mostly full-time going further down the DNS rabbit hole, reading RFCs, talking to experts, and writing, recording.

::card
---
title: The DNS course for developers
icon: i-eos-icons:dns
to: https://www.nslookup.io/dns-course/
target: _blank
---
Never be confused about DNS again.
::

## Here's how this course works.

I've set this course up in six modules:

- DNS from first principles
- Domain names
- Operational DNS
- Types of DNS records
- Email and the DNS
- Debugging DNS issues

::tip
Each module ends with a quiz for you to test your knowledge.
::

::important
Later lessons build on knowledge in previous lessons, so it's best to take the course in order.
::

## When you finish this course, you will:

- Have a foundational understanding of DNS,
- Be able to configure DNS records with confidence,
- Be able to debug DNS issues effectively.

## Glossary

| Term                          | Definition |
|-------------------------------|-----------|
| A record                      | A type of DNS record that maps a domain name to an IPv4 address. |
| AAAA record                   | A type of DNS record that maps a domain name to an IPv6 address. |
| Accredited registrar           | A registrar that passed ICANN's accreditation process. They have a Registrar Accreditation Agreement with ICANN. |
| Add grace period               | A 5-day period in which domain name registrations can be undone, and the cost reversed. |
| Anycast                        | A networking methodology that allows multiple servers to listen to the same IP address. |
| API                            | Application programming interface. A way for two programs to communicate. |
| ASCII                          | American Standard Code for Information Interchange. A character encoding with western characters. |
| Auth code                      | Synonymous to transfer code. |
| Auth info                      | Synonymous to transfer code. |
| Authoritative DNS server       | A DNS server that has authority (ownership) over a DNS zone. Only this server can change DNS records for domain names in that zone. |
| Authority                      | A server that manages a domain name is said to have authority over that zone. |
| Auto-renew                     | A setting at registrars that automatically renews a domain name. Prevents accidental drops but can cause accidental charges. |
| Auto-renew grace period        | A 0 to 45 day period after domain expiry in which a domain owner can still renew a domain name for the usual cost. |
| AXFR                           | A DNS query type that requests all the DNS records for a specific DNS zone. |
| Brand TLD                      | A TLD that a brand owns, usually not allowing external registrations (e.g., .google, .accenture). |
| Caching                        | Remembering data for a specified period to use it later without re-requesting it. |
| ccTLD                          | Country code top-level domain. A 2-letter TLD operated by a country (e.g., .nl, .io). |
| Character encoding             | Assigning numbers to letters and symbols for computer storage and transmission. |
| Child zone                     | A zone directly below another zone in the DNS tree. |
| Client                         | A software component capable of making DNS requests. |
| DNS                            | The Domain Name System. Stores all domain names and associated data. |
| DNS protocol                   | The network protocol of the DNS. |
| DNS query                      | A request message in the DNS protocol (e.g., "What is the IPv4 address of example.com?"). |
| DNS request                    | Synonymous with DNS query. |
| DNS root                       | The uppermost domain name in the DNS tree. All domain names originate from here. |
| DNS server                     | A server that responds to DNS queries. |
| DNS Tree                       | The logical representation of the domain name space as a tree structure. |
| Database replication           | A mechanism to keep data synchronized across multiple databases. |
| Debugging                      | The manual process of investigating and fixing an issue. |
| Delegation                     | Synonymous with "Zone delegation." |
| Design pattern                 | A commonly used software design to achieve a specific goal. |
| Dig                            | A command-line tool to query DNS records from a server. |
| Distributed system             | A system whose components are located on different servers. |
| Distribution                   | A design pattern that composes multiple servers into a single system. |
| Domain expiry                  | The moment a domain name registration period ends. |
| Domain hijacking               | Unauthorized transfer of a domain name. |
| Domain lock                    | A registrar setting that prevents domain transfer. |
| Domain name                    | A human-friendly text string representing a node in the DNS tree (e.g., wikipedia.org). |
| Domain name space              | The set of all possible domain names. |
| Domain transfer                | The transfer of domain ownership from one party to another. |
| Drop-catching                  | Attempting to register a domain name immediately after it expires. |
| Expire time                    | The time after which a secondary DNS server considers a zone offline if it cannot be transferred. |
| Fault-tolerance                | The ability of a system to continue functioning despite failures. |
| Federated database             | A database consisting of smaller merged databases. |
| Federation                     | See "federated database." |
| Functional second-level domain | A third-level domain where its parent is a public suffix domain. |
| GDPR                           | General Data Protection Regulation. An EU regulation on data privacy. |
| Generic restricted TLD         | A generic TLD limited to a specific purpose. |
| Grace period                   | A period in which an action can be reversed. |
| gTLD                           | Generic top-level domain (e.g., .com, .org) not tied to a country. |
| IANA                           | Internet Assigned Numbers Authority. Manages the DNS root zone. |
| ICANN                          | Internet Corporation for Assigned Names and Numbers. Oversees the DNS. |
| IDN                            | International domain name. A domain that starts with xn-- and can be translated to Unicode characters. |
| Infrastructure TLD             | .arpa, the only infrastructure TLD, used for IP to domain name lookups. |
| IP address                     | A numerical label (e.g., 192.0.2.1) assigned to devices on a network. |
| IPv4 address                   | A 32-bit IP address. |
| IPv6 address                   | A 128-bit IP address. |
| ISO 3166                       | Standard for country codes, officially called "Codes for the representation of names of countries and their subdivisions." |
| IXFR                           | A DNS query type that requests an incremental zone transfer. |
| Incremental zone transfer      | A DNS zone transfer method that transmits only changes instead of all data. |
| JSON                           | JavaScript Object Notation. A human-readable file format for data transmission. |
| Key-value store                | A database that stores data objects referenced by keys. |
| Label                          | A text string in the DNS tree (e.g., "en", "wikipedia", "org" in en.wikipedia.org). |
| Latin characters               | The alphabet containing letters A-Z. |
| Leader/follower architecture   | A distributed systems pattern where a leader server replicates data to follower servers. |
| Master                         | Synonymous with "leader" in leader/follower architecture. |
| Multi-tenancy                  | A system's ability to support multiple independent users. |
| NS record                      | A DNS record that maps a domain name to authoritative DNS servers. |
| Name server                    | A DNS server. See "Authoritative DNS server" and "NS record." |
| Network protocol               | A formal set of network messages that enable communication between systems. |
| Notify query                   | A DNS query a leader sends to a follower to signal a zone data change. |
| Operating system               | Software that manages computer hardware and software resources (e.g., Windows, Linux). |
| Parent zone                    | The zone one level above the current zone in the DNS tree. |
| Pending delete                 | The state before a domain is permanently deleted. |
| Performance                    | The speed (throughput and latency) of a system. |
| Primary DNS server             | The leader in a leader/follower authoritative DNS deployment. |
| Public suffix domain           | A domain where users can register subdomains (e.g., .org, .co.uk). |
| Punycode                       | A representation of Unicode characters using ASCII characters. |
| Quad A record                  | Synonymous with "AAAA record." |
| Rate limiting                  | Rejecting requests when a requester exceeds a predefined rate. |
| RDAP                           | Registration Data Access Protocol. Replaces WHOIS. |
| RDDS                           | Registration Data Directory Services. A service for looking up domain registration data. |
| Recursion                      | A concept where a process is defined in terms of itself. |
| Redemption grace period        | A period after domain expiration where it can still be redeemed (higher fee). |
| Refresh time                   | A field in SOA records that defines how often secondary servers request updates. |
| Registrar                    | An organization that sells domain name registrations. |
| Registrar Accreditation Agreements | A contract between ICANN and a registrar. |
| Registry                     | An organization that operates a TLD. |
| Replication                  | A design pattern where data is stored on multiple parts of a system in order to achieve fault tolerance and scalability. |
| Resolver                     | A DNS client that sometimes also caches responses. |
| RESTful                      | A web interface that conforms to the Representational State Transfer (REST) standard. |
| Retry time                   | A field in a SOA record that specifies how long secondary servers should wait before retrying zone transfer when it fails. |
| RFC                          | Request for Comments. A document that standardizes (internet) technology. |
| Root server                  | A DNS server that is authoritative for the root zone. |
| Root WHOIS server            | The WHOIS server that hosts information about TLDs. It is run by IANA at whois.iana.org. |
| Root zone                    | The DNS zone that contains the DNS root. |
| SOA record                   | All DNS zones begin with a Start Of Authority (SOA) record. The SOA record states that authority for a zone starts at a particular point in the global tree of DNS names. This record also contains configuration data for zone transfer. |
| Scalability                  | The ability for a system to accommodate growing usage. |
| Secondary DNS server         | A follower in a leader/follower deployment of a set of authoritative DNS servers. |
| Second-level domain          | A domain name directly below a TLD. Examples are co.uk and wikipedia.org. |
| Serial number                | A field in a SOA record that acts as a version number of zone data. This allows secondary DNS servers to know if they have the most up-to-date data. |
| Sponsored TLD                | A domain name that serves a specific community or industry. |
| Staleness                    | Cached DNS records that have been updated are called stale records. |
| Start of authority record    | Synonymous with "SOA record". |
| Subdomain                    | A domain name immediately below another domain name in the DNS tree. |
| TCP                          | A network protocol that allows reliable transfer of data. |
| Test TLD                     | A TLD with the specific purpose of testing DNS software and infrastructure. There are currently 11 test TLDs — all for testing IDNs. |
| Thick registry               | A registry that maintains WHOIS data. |
| Thin registry                | A registry that doesn't maintain WHOIS data. WHOIS data for domains under this registry are maintained by registrars. |
| Third-level domain           | A domain name directly below a second-level domain. Examples are en.wikipedia.org and bbc.co.uk. |
| TLD                          | See "Top-level domain". |
| Top-level domain             | A domain name directly below the DNS root. Examples are com, org, and nl. |
| Trace                        | The process of looking up DNS records, starting at the DNS root, and working its way down until it lands on the requested domain name. |
| Transfer code                | A code that you can enter at a different registrar to move a domain name there. |
| Transfer contact email       | The email address a registrar or registry has as an authorized person to transfer a domain name. This is entered upon registration and can be changed at any time. It is sometimes shown in WHOIS responses. |
| Tree structure               | A data structure that stores the domain name space. It starts at a single root node and flows downwards through labels. |
| UDP                          | A network protocol that prioritizes performance over reliability. |
| UDRP                         | Uniform Domain-Name Dispute-Resolution. A way for trademark holders to take ownership of a domain name if it was registered in bad faith. |
| Unicode                      | A character encoding for most of the world's writing systems. |
| Verisign                     | A commercial organization that runs two of the DNS root servers. |
| Well-known                   | Something is said to be well-known when it is common knowledge that does not need to be shared or looked up. |
| WHOIS                        | A network protocol to look up domain name registration data. |
| Wireshark                    | Software to capture and inspect local network traffic. |
| Zone                         | A DNS zone is a domain name and all domain names below it, excluding any domain names that have been delegated. |
| Zone delegation              | The process of handing over the authority of a subdomain to a child zone. |
| Zone file                    | A text file in a formally specified syntax that can be interpreted by DNS software. It contains DNS records. |
| Zone transfer                | A process to synchronize DNS data between primary and secondary DNS servers. |
