---
title: Windows File Systems
description: NTFS, FAT32, permissions en toegangscontrole in Windows
---

## 💾 Windows File System Overzicht

Er zijn vijf types Windows file systems: **FAT12**, **FAT16**, **FAT32**, **NTFS**, en **exFAT**. FAT12 en FAT16 worden niet meer gebruikt op moderne Windows systemen. Deze training richt zich voornamelijk op het **NTFS file system**, met een korte bespreking van FAT32 en exFAT.

### FAT32 (File Allocation Table)

**FAT32** wordt veel gebruikt op verschillende opslagapparaten zoals USB-sticks en SD-kaarten, maar kan ook voor harde schijven worden gebruikt. De "32" verwijst naar het feit dat FAT32 **32 bits data** gebruikt voor het identificeren van data clusters op een opslagapparaat.

::tip
**Voordelen van FAT32:**
- **Device compatibility** — Werkt op computers, digitale camera's, gaming consoles, smartphones, tablets en meer
- **Cross-platform support** — Functioneert op alle Windows versies vanaf Windows 95, plus MacOS en Linux
::

::caution
**Nadelen van FAT32:**
- Kan alleen gebruikt worden met bestanden **kleiner dan 4GB**
- Geen ingebouwde data protection of file compression features
- Vereist third-party tools voor file encryption
::

### NTFS (New Technology File System)

**NTFS** is het standaard Windows file system sinds Windows NT 3.1. NTFS compenseert de tekortkomingen van FAT32 en heeft betere ondersteuning voor metadata en verbeterde performance door betere data structurering.

::important
**Voordelen van NTFS:**
- **Betrouwbaar** — Herstelt file system consistency bij system failures of stroomuitval
- **Security** — Granulaire permissions op zowel files als folders
- **Grote partities** — Ondersteunt zeer grote partities
- **Journaling** — File modificaties (toevoegen, wijzigen, verwijderen) worden gelogd
::

::note
**Nadelen van NTFS:**
- De meeste mobiele apparaten ondersteunen NTFS niet standaard
- Oudere media-apparaten (TV's, digitale camera's) bieden geen ondersteuning voor NTFS
::

## 🔐 NTFS Permissions

Het NTFS file system heeft veel basis en geavanceerde permissions. De belangrijkste permission types zijn:

| Permission Type | Beschrijving |
|----------------|--------------|
| **Full Control** | Lezen, schrijven, wijzigen en verwijderen van files/folders |
| **Modify** | Lezen, schrijven en verwijderen van files/folders |
| **List Folder Contents** | Bekijken en listen van folders en subfolders, en uitvoeren van files. Alleen folders erven deze permission |
| **Read and Execute** | Bekijken en listen van files en subfolders, en uitvoeren van files. Files en folders erven deze permission |
| **Write** | Toevoegen van files aan folders en subfolders, en schrijven naar een file |
| **Read** | Bekijken en listen van folders en subfolders, en bekijken van file contents |
| **Traverse Folder** | Verplaatsen door folders om andere files/folders te bereiken, zelfs zonder toegang tot parent folders. Bijvoorbeeld: Een user kan `c:\users\bsmith\documents\webapps\backups\backup_02042020.zip` bereiken, zelfs zonder permission om de tussenliggende directories te listen |

### Permission Inheritance

Files en folders **erven NTFS permissions van hun parent folder** voor eenvoudig beheer. Administrators hoeven niet expliciet permissions te zetten voor elke file en folder, wat extreem tijdrovend zou zijn.

::note
Als permissions expliciet moeten worden gezet, kan een administrator **permissions inheritance uitschakelen** voor de benodigde files en folders, en dan permissions direct instellen op elk item.
::

## 🛠️ Integrity Control Access Control List (icacls)

NTFS permissions kunnen worden beheerd via de **File Explorer GUI** onder de Security tab. Daarnaast biedt de **`icacls` utility** vanaf de command line een fijne granulariteit over NTFS file permissions.

### NTFS Permissions Bekijken

Permissions op een directory bekijken kan met `icacls` vanuit de working directory, of met een absolute path:

```cmd
C:\htb> icacls c:\windows
c:\windows NT SERVICE\TrustedInstaller:(F)
           NT SERVICE\TrustedInstaller:(CI)(IO)(F)
           NT AUTHORITY\SYSTEM:(M)
           NT AUTHORITY\SYSTEM:(OI)(CI)(IO)(F)
           BUILTIN\Administrators:(M)
           BUILTIN\Administrators:(OI)(CI)(IO)(F)
           BUILTIN\Users:(RX)
           BUILTIN\Users:(OI)(CI)(IO)(GR,GE)
           CREATOR OWNER:(OI)(CI)(IO)(F)
           APPLICATION PACKAGE AUTHORITY\ALL APPLICATION PACKAGES:(RX)
           APPLICATION PACKAGE AUTHORITY\ALL APPLICATION PACKAGES:(OI)(CI)(IO)(GR,GE)
           APPLICATION PACKAGE AUTHORITY\ALL RESTRICTED APPLICATION PACKAGES:(RX)
           APPLICATION PACKAGE AUTHORITY\ALL RESTRICTED APPLICATION PACKAGES:(OI)(CI)(IO)(GR,GE)

Successfully processed 1 files; Failed processing 0 files
```

### Inheritance Settings

Bepalen hoe permissions worden doorgegeven.

Het resource access level staat achter elke user in de output. Mogelijke **inheritance settings** zijn:

| Setting | Betekenis |
|---------|-----------|
| **(CI)** | Container inherit |
| **(OI)** | Object inherit |
| **(IO)** | Inherit only |
| **(NP)** | Do not propagate inherit |
| **(I)** | Permission inherited from parent container |

::important
In het bovenstaande voorbeeld heeft het **NT AUTHORITY\SYSTEM** account object inherit **(OI)**, container inherit **(CI)**, inherit only **(IO)**, en full access permissions **(F)**. Dit betekent dat dit account **volledige controle** heeft over alle file system objects in deze directory en subdirectories.
::

### Access Levels 

Access Permissions

| Permission | Betekenis |
|-----------|-----------|
| **F** | Full access |
| **D** | Delete access |
| **N** | No access |
| **M** | Modify access |
| **RX** | Read and execute access |
| **R** | Read-only access |
| **W** | Write-only access |

## ⚙️ Permissions Toevoegen en Verwijderen

We kunnen permissions toevoegen en verwijderen via de command line met `icacls`.

### Voorbeeld: Permissions Bekijken voor C:\Users

```cmd
C:\htb> icacls c:\Users
c:\Users NT AUTHORITY\SYSTEM:(OI)(CI)(F)
         BUILTIN\Administrators:(OI)(CI)(F)
         BUILTIN\Users:(RX)
         BUILTIN\Users:(OI)(CI)(IO)(GR,GE)
         Everyone:(RX)
         Everyone:(OI)(CI)(IO)(GR,GE)

Successfully processed 1 files; Failed processing 0 files
```

In dit voorbeeld heeft de user **joe** geen write permissions op `C:\Users`.

### Permissions Toekennen met icacls

Met het commando `icacls c:\users /grant joe:f` kunnen we de user **joe** full control geven over de directory.

::warning
Omdat **(OI)** en **(CI)** niet werden opgenomen in het commando, heeft de user joe **alleen rechten over de `c:\users` folder**, maar niet over de user subdirectories en files daarin.
::

```cmd
C:\htb> icacls c:\users /grant joe:f
processed file: c:\users
Successfully processed 1 files; Failed processing 0 files
```

**Resultaat na het grant commando:**

```cmd
C:\htb> icacls c:\users
c:\users WS01\joe:(F)
         NT AUTHORITY\SYSTEM:(OI)(CI)(F)
         BUILTIN\Administrators:(OI)(CI)(F)
         BUILTIN\Users:(RX)
         BUILTIN\Users:(OI)(CI)(IO)(GR,GE)
         Everyone:(RX)
         Everyone:(OI)(CI)(IO)(GR,GE)

Successfully processed 1 files; Failed processing 0 files
```

### Permissions Verwijderen

Permissions kunnen worden ingetrokken met het commando:

```cmd
icacls c:\users /remove joe
```

### Geavanceerde icacls Functionaliteit

**`icacls` is zeer krachtig** en kan gebruikt worden in een domein-omgeving om:
- Specifieke permissions geven aan bepaalde users of groups over een file of folder
- Expliciet toegang weigeren
- Inheritance permissions in- of uitschakelen
- Directory/file ownership wijzigen

::tip
Een volledige lijst van `icacls` command-line argumenten en gedetailleerde permission settings is beschikbaar op:
**[https://ss64.com/nt/icacls.html](https://ss64.com/nt/icacls.html)**
::
