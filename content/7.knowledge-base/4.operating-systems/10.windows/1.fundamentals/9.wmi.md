---
title: WMI
description: Windows Management Istrumentation architecture. WMIC commands, Powershell WMI modules
toc:
  depth: 1
---

## 🔍 Windows Management Instrumentation (WMI)

**WMI** is een subsysteem van PowerShell dat system administrators **krachtige tools** biedt voor system monitoring en management. Het doel van WMI is om **device en application management** te consolideren over corporate networks.

::important
**WMI Core Information:**
- WMI is een **core onderdeel** van het Windows besturingssysteem
- **Pre-installed** sinds Windows 2000
- Draait automatisch bij system boot
- Biedt **lokale én remote** management capabilities
::

### WMI Primaire Doelen

WMI consolideert de volgende functionaliteiten:
- **Device management** — Hardware monitoring en configuratie
- **Application management** — Software deployment en monitoring
- **System configuration** — Remote configuratie wijzigingen
- **Performance monitoring** — Real-time system metrics
- **Event monitoring** — System en application events

## 🏗️ WMI Architecture Components

WMI bestaat uit verschillende **interconnected components** die samenwerken om system management functionaliteit te bieden.

| Component | Beschrijving |
|-----------|--------------|
| **WMI Service** | Het **Windows Management Instrumentation process** (`winmgmt`) dat automatisch draait bij boot en fungeert als **intermediary** tussen WMI providers, de WMI repository en managing applications |
| **Managed Objects** | Alle **logische of fysieke componenten** die kunnen worden beheerd door WMI (bijv. files, processes, services, users, network adapters) |
| **WMI Providers** | Objects die **events/data monitoren** gerelateerd aan specifieke managed objects. Providers fungeren als **bridge** tussen managed objects en WMI |
| **Classes** | Gebruikt door WMI providers om **data door te geven** aan de WMI service. Elke class representeert een manageable entity (bijv. `Win32_Process`, `Win32_Service`) |
| **Methods** | Functies **attached aan classes** die acties mogelijk maken (bijv. start/stop processen op remote machines, create/delete users) |
| **WMI Repository** | Een **database** (`OBJECTS.DATA`) die alle **statische data** opslaat gerelateerd aan WMI. Locatie: `C:\Windows\System32\wbem\Repository\` |
| **CIM Object Manager** | Het systeem dat **data opvraagt** bij WMI providers en deze **returned** naar de applicatie die het request doet. CIM = Common Information Model |
| **WMI API** | Stelt applicaties in staat om **toegang te krijgen** tot de WMI infrastructure via programming interfaces |
| **WMI Consumer** | Stuurt **queries** naar objects via de CIM Object Manager (bijv. PowerShell scripts, WMIC commands, custom applications) |

::note
**WMI Repository Database:**
De WMI repository (`OBJECTS.DATA`) bevat:
- **Class definitions** — Schema voor alle WMI classes
- **Static data** — Permanent configuration informatie
- **Namespace organization** — Hiërarchische structuur van WMI data

Locatie: `C:\Windows\System32\wbem\Repository\OBJECTS.DATA`
::

## 🛠️ WMI Use Cases

WMI biedt **uitgebreide functionaliteit** voor system management:

| Use Case | Beschrijving | Voorbeeld |
|----------|--------------|-----------|
| **Status Information** | Lokale/remote systeem informatie verzamelen | OS version, hardware specs, running processes |
| **Security Configuration** | Security settings configureren op remote machines/applications | Firewall rules, audit policies, user rights |
| **User & Group Permissions** | User en group permissions instellen en wijzigen | Add users to groups, modify access rights |
| **System Properties** | System properties instellen/wijzigen | Computer name, domain membership, environment variables |
| **Code Execution** | Arbitrary code uitvoeren op local/remote systems | Process creation, script execution |
| **Process Scheduling** | Processen schedulen voor later of recurring execution | Scheduled tasks via WMI |
| **Logging Configuration** | Logging instellen en configureren | Event log settings, audit configuration |
| **Software Deployment** | Software installeren op remote systems | MSI installations, package deployment |
| **Inventory Management** | Hardware en software inventarisatie | Installed software, hardware components |

::warning
**Security Implication:**
WMI's **code execution** en **remote management** capabilities maken het een **primary tool** voor zowel:
- ✅ **Legitimate administration** — System management en automation
- ❌ **Offensive operations** — Lateral movement, persistence, reconnaissance
::

## 💻 WMIC Command-Line Interface

**WMIC** (WMI Command-Line) is de command-line interface voor WMI. Het kan gebruikt worden voor **interactive shells** of **direct command execution**.

::caution
**WMIC Deprecation:**
Microsoft heeft **WMIC deprecated** in recente Windows versies. PowerShell `Get-CimInstance` en `Invoke-CimMethod` zijn de **aanbevolen alternatieven**.

Echter, WMIC is nog steeds **aanwezig** in veel Windows systemen en wordt **frequent gebruikt** in legacy scripts en offensive tooling.
::

### WMIC Interactive Shell

WMIC openen door simpelweg `wmic` te typen:

```cmd
C:\htb> wmic
wmic:root\cli>
```

### WMIC Direct Commands

**Hostname ophalen:**
```cmd
C:\htb> wmic computersystem get name

Name
DESKTOP-ABC123
```

**OS informatie:**
```cmd
C:\htb> wmic os list brief

BuildNumber  Organization  RegisteredUser  SerialNumber             SystemDirectory      Version
19041                      Owner           00123-00123-00123-AAOEM  C:\Windows\system32  10.0.19041
```

### WMIC Global Switches

```cmd
C:\htb> wmic /?

WMIC is deprecated.

[global switches] <command>

The following global switches are available:
/NAMESPACE           Path for the namespace the alias operate against.
/ROLE                Path for the role containing the alias definitions.
/NODE                Servers the alias will operate against.
/IMPLEVEL            Client impersonation level.
/AUTHLEVEL           Client authentication level.
/LOCALE              Language id the client should use.
/PRIVILEGES          Enable or disable all privileges.
/TRACE               Outputs debugging information to stderr.
/RECORD              Logs all input commands and output.
/INTERACTIVE         Sets or resets the interactive mode.
/FAILFAST            Sets or resets the FailFast mode.
/USER                User to be used during the session.
/PASSWORD            Password to be used for session login.
/OUTPUT              Specifies the mode for output redirection.
/APPEND              Specifies the mode for output redirection.
/AGGREGATE           Sets or resets aggregate mode.
/AUTHORITY           Specifies the <authority type> for the connection.
/?[:<BRIEF|FULL>]    Usage information.
```

### WMIC Aliases, Verbs, Adverbs

**WMIC gebruikt een gestructureerde [syntax](https://docs.microsoft.com/en-us/windows/win32/wmisdk/wmic):**

| Element | Beschrijving | Voorbeeld |
|---------|--------------|-----------|
| **Alias** | Shortcut voor WMI class | `process`, `service`, `os`, `computersystem` |
| **Verb** | Actie om uit te voeren | `LIST`, `GET`, `CALL`, `CREATE`, `DELETE` |
| **Adverb** | Modificeert de verb | `BRIEF`, `FULL`, `SYSTEM`, `STATUS` |
| **Switch** | Additional parameters | `/FORMAT`, `/NODE`, `/USER` |

**Voorbeelden:**

```cmd
# List running processes (brief)
wmic process list brief

# Get specific process by name
wmic process where name="explorer.exe" get processid,commandline

# Call method to terminate process
wmic process where processid="1234" call terminate

# Remote execution
wmic /node:"192.168.1.100" /user:"DOMAIN\admin" process call create "cmd.exe"
```

## ⚡ PowerShell WMI Modules

PowerShell biedt **krachtige WMI integration** via meerdere cmdlets.

### [Get-WmiObject](https://docs.microsoft.com/en-us/powershell/module/microsoft.powershell.management/get-wmiobject?view=powershell-5.1) (Legacy)

**`Get-WmiObject`** wordt gebruikt om instances van WMI classes te krijgen of informatie over beschikbare classes.

::note
**Legacy vs. Modern:**
- **Legacy:** `Get-WmiObject` (deprecated maar nog veel gebruikt)
- **Modern:** `Get-CimInstance` (aanbevolen vanaf PowerShell 3.0+)

`Get-CimInstance` gebruikt **WS-Management protocol** in plaats van DCOM, wat beter is voor **firewall traversal** en **security**.
::

**OS informatie ophalen:**
```powershell
PS C:\htb> Get-WmiObject -Class Win32_OperatingSystem | select SystemDirectory,BuildNumber,SerialNumber,Version | ft

SystemDirectory     BuildNumber SerialNumber            Version
---------------     ----------- ------------            -------
C:\Windows\system32 19041       00123-00123-00123-AAOEM 10.0.19041
```

**Alle WMI classes in namespace:**
```powershell
Get-WmiObject -List
```

**Processes ophalen:**
```powershell
Get-WmiObject -Class Win32_Process | Select Name, ProcessId, CommandLine | ft
```

**Remote query:**
```powershell
Get-WmiObject -Class Win32_Process -ComputerName "remote-pc" -Credential (Get-Credential)
```

### [Invoke-WmiMethod](https://docs.microsoft.com/en-us/powershell/module/microsoft.powershell.management/invoke-wmimethod?view=powershell-5.1)

**`Invoke-WmiMethod`** wordt gebruikt om **methods van WMI objects** aan te roepen.

**File renamen:**
```powershell
PS C:\htb> Invoke-WmiMethod -Path "CIM_DataFile.Name='C:\users\public\spns.csv'" -Name Rename -ArgumentList "C:\Users\Public\kerberoasted_users.csv"

__GENUS          : 2
__CLASS          : __PARAMETERS
__SUPERCLASS     :
__DYNASTY        : __PARAMETERS
__RELPATH        :
__PROPERTY_COUNT : 1
__DERIVATION     : {}
__SERVER         :
__NAMESPACE      :
__PATH           :
ReturnValue      : 0
PSComputerName   :
```

::tip
**ReturnValue Interpretation:**
- **0** = Success
- **Non-zero** = Error (specifieke error code afhankelijk van method)
::

**Process starten:**
```powershell
Invoke-WmiMethod -Class Win32_Process -Name Create -ArgumentList "notepad.exe"
```

**Service stoppen:**
```powershell
Invoke-WmiMethod -Path "Win32_Service.Name='Spooler'" -Name StopService
```

**Remote process execution:**
```powershell
Invoke-WmiMethod -Class Win32_Process -Name Create -ArgumentList "cmd.exe /c whoami > C:\temp\output.txt" -ComputerName "remote-pc" -Credential (Get-Credential)
```

### Get-CimInstance & Invoke-CimMethod (Modern)

**Modern WMI alternatieven** die **CIM/WS-Management** gebruiken:

```powershell
# Get-CimInstance (modern alternative voor Get-WmiObject)
Get-CimInstance -ClassName Win32_OperatingSystem

# Invoke-CimMethod (modern alternative voor Invoke-WmiMethod)
Invoke-CimMethod -ClassName Win32_Process -MethodName Create -Arguments @{CommandLine="notepad.exe"}

# Remote via CIM session
$session = New-CimSession -ComputerName "remote-pc" -Credential (Get-Credential)
Get-CimInstance -ClassName Win32_Process -CimSession $session
```

::important
**Voordelen van CIM Cmdlets:**
- Gebruikt **WS-Management (WinRM)** protocol in plaats van DCOM
- Betere **firewall compatibility** (WinRM poort 5985/5986 vs. DCOM dynamic ports)
- **Persistente sessies** via `New-CimSession`
- **Verbeterde error handling**
- **Cross-platform support** (PowerShell Core/7+)
::
