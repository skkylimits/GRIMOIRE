---
title: 3D
description: Going to the 3 dimensional plane
---

## **🎬 3D World System - The Sequel** 🌍🎥

If our 2D UI was a movie set, then the 3D world is the **blockbuster sequel**—bigger, more immersive, and packed with dynamic performances. At the heart of this experience is the `World.js` file, which acts like a **Scene Director**, managing complex interactions between 3D environments, animated characters, and thematic rooms.

### **🌍 Your 3D Cast & Crew** (The 3D Players)

| **Role**                    | **File / Class**      | **Listens to** | **Triggers** | **Notes**                                                  |
| --------------------------- | --------------------- | -------------- | ------------ | ---------------------------------------------------------- |
| 🎬 **3D Scene Director**    | `World.js`            | `loaded`       | `update()`   | Orchestrates the entire 3D world after resources are ready |
| 🌤️ **Environment**         | `Environment.js`      | —              | —            | Sets up lighting, fog, and environmental effects           |
| 🏠 **Room Setup**           | `Room.js`             | —              | —            | Main environment for the 3D landing page                   |
| 💻 **Desktops**             | `Desktops.js`         | —              | —            | Adds interactive desktop elements inside the room          |
| 🧍 **Character Body**       | `Body.js`             | —              | —            | Loads and positions the full character model               |
| 😐 **Character Face**       | `Face.js`             | —              | —            | Handles the facial mesh and materials                      |
| 🕺 **Character Animations** | `Animations.js`       | `update()`     | —            | Manages and updates character animations in real-time      |
| 🕰️ **Character Intervals** | `Intervals.js`        | —              | —            | Triggers timed character expressions or motions            |
| 🔬 **Lab Scene**            | `Lab.js`              | —              | —            | Creates a futuristic lab environment                       |
| 🧪 **Tube**                 | `Tube.js`             | —              | —            | Central glass tube model in the lab scene                  |
| 💭 **Bubbles**              | `Bubbles.js`          | —              | —            | Floating particles inside the lab tube                     |
| 💌 **Contact Scene**        | `ContactScene.js`     | —              | —            | Sets up a 3D contact scene                                 |
| 🎞️ **Contact Animation**   | `ContactAnimation.js` | `resize`       | —            | Animates the contact section in sync with layout changes   |

### **🌟 Behind the Scenes - How the 3D World Boots Up**

Once the stage (i.e., resources) is set by `Experience.js`, the `World.js` director springs into action. Here’s how it plays out:

1. **Resources First**: The entire cast waits until the set is loaded (`resources.on('loaded')`).
2. **Scene Setup Begins**:

   * **Environment effects** are initialized first.
   * **Landing room** and **desktop props** are placed.
   * The **main character** is rigged with body parts, face, and real-time animations.
   * **Lab** is built, tube and bubbles included.
   * Finally, the **Contact scene** is staged and animated.
3. **Update Loop**: Every frame, animations are updated (e.g., blinking, gestures, idle motions).

### **🎬 3D World Hierarchy - Who Reports to Who?**

```
World.js (🎬 3D Scene Director)
├── Environment.js (🌤️ Environment Setup)
├── Landing Page
│   ├── Room.js (🏠 Room)
│   └── Desktops.js (💻 Desktops)
├── Character
│   ├── Face.js (😐 Face)
│   ├── Body.js (🧍 Body)
│   ├── Animations.js (🕺 Animations)
│   └── Intervals.js (🕰️ Expressions)
├── Lab
│   ├── Lab.js (🔬 Lab Environment)
│   ├── Tube.js (🧪 Tube)
│   └── Bubbles.js (💭 Particles)
└── Contact
    ├── ContactScene.js (💌 Scene)
    └── ContactAnimation.js (🎞️ Animation FX)
```

### **📽️ Real-Time Flow - How 3D Plays Out**

1. **Initialization**: World waits for the `resources.on('loaded')` event before spawning any scene objects.
2. **Setup Phase**: All objects, props, characters, and environments are constructed in memory.
3. **Live Update**: On every frame, the `update()` method is called, driving real-time behaviors like:

   * Character animations
   * Bubble particle physics
   * Any live interactions (to be expanded)
