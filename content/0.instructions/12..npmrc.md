---
title: .npmrc
icon: 'i-octicon:file-24'
description: The .npmrc file—silent but powerful, a config node controlling the flow of dependencies and setting the rules for your package’s future.
---

The `.npmrc` file is a configuration file for npm (Node Package Manager), allowing developers to specify custom settings such as registry URLs, authentication tokens, and various npm behavior options for a project or user.

::callout
---
icon: i-mdi:nuxt
target: _blank
to: https://pnpm.io/npmrc
---
[Learn more about how the `.npmrc` file work]()
::

## shamefully-hoist

Imagine you're building a toy set, with blocks usually sorted into separate boxes (like pnpm). Some older sets expect all blocks in one big box (like npm). Using shamefully-hoist=true mixes all blocks together, helping those older sets work smoothly, even if it's a bit messy.

```[.npmrc]
shamefully-hoist=true
```

