{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Normalize Image Paths In Markdown",
			"type": "shell",
			"command": "npm run normalize ${file}",
			"problemMatcher": [],
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"options": {
				"cwd": "${workspaceFolder}"
			}
		}
	]
}
