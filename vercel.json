{
	"ignoreCommand": "BRANCH=$(echo \"$VERCEL_GIT_BRANCH\" | tr -d '\\r\\n'); echo \"DEBUG: VERCEL_GIT_BRANCH='$VERCEL_GIT_BRANCH' | BRANCH='$BRANCH'\"; if [ \"$BRANCH\" != \"main\" ]; then echo 'Skipping non-main branch'; exit 0; fi"
}
